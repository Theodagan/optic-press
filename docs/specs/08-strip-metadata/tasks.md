# Strip Metadata Tasks

## Feature Dependencies
- Requires docs/specs/02-image-processing-foundation/ before implementation.
- Requires docs/specs/01-app-shell-workspace/ before implementation.

1. [ ] Define strip metadata settings and summary model. → needs: —
2. [ ] Implement canvas redraw metadata removal flow. → needs: —
3. [ ] Implement optional metadata summary extraction. → needs: 1
4. [ ] Build Strip Metadata workspace controls. → needs: 1
5. [ ] Wire output format options and downloads. → needs: 2, 4
6. [ ] Add tests for settings, redraw flow, and summary rendering. → needs: 1, 2, 3, 4
