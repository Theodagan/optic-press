# App Shell Workspace Tasks

1. [x] use the angular cli to bootstrap the app conforming to architecture described in the steering docs → needs: —
2. [x] Create Angular routes for homepage and each tool workspace. → needs: —
3. [x] Implement homepage tool grid from PRD tool list. → needs: 2
4. [x] Implement shared upload zone with drag/drop, file picker, and paste. → needs: 2
5. [x] Implement shared image card with output status and size stats. → needs: 2
6. [x] Implement shared download bar for aggregate stats and ZIP action hook. → needs: 5
7. [x] Implement shared output options collapsible. → needs: 2
8. [x] Wire placeholder workspaces for all extracted tools. → needs: 3, 4, 5, 7
9. [x] Add route/component smoke tests. → needs: 8
