# Image Processing Foundation Tasks

1. [x] Define shared image job, settings, preset, and output models. → needs: —
2. [x] Implement AVIF support detection utility. → needs: —
3. [x] Implement size formatting and savings utilities. → needs: —
4. [x] Implement shared image decode/draw/encode service. → needs: 1, 2
5. [x] Add resize dimension helpers with upscale prevention support. → needs: 1
6. [x] Implement ZIP generation service with `jszip`. → needs: 1
7. [x] Add content detection helper for downsampled variance analysis. → needs: 4
8. [x] Add worker or OffscreenCanvas path for expensive processing. → needs: 4
9. [x] Add unit tests for utilities and core processor behavior. → needs: 2, 3, 4, 5, 6, 7
