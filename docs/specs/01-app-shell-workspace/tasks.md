# App Shell Workspace Tasks

1. Create Angular routes for homepage and each tool workspace. → needs: —
2. Implement homepage tool grid from PRD tool list. → needs: 1
3. Implement shared upload zone with drag/drop, file picker, and paste. → needs: 1
4. Implement shared image card with output status and size stats. → needs: 1
5. Implement shared download bar for aggregate stats and ZIP action hook. → needs: 4
6. Implement shared output options collapsible. → needs: 1
7. Wire placeholder workspaces for all extracted tools. → needs: 2, 3, 4, 6
8. Add route/component smoke tests. → needs: 7
