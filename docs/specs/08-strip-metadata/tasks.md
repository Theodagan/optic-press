# Strip Metadata Tasks

## Feature Dependencies
- Requires docs/specs/02-image-processing-foundation/ before implementation.
- Requires docs/specs/01-app-shell-workspace/ before implementation.

1. [x] Define strip metadata settings and summary model. → needs: —
2. [x] Implement canvas redraw metadata removal flow. → needs: —
3. [x] Implement optional metadata summary extraction. → needs: 1
4. [x] Build Strip Metadata workspace controls. → needs: 1
5. [x] Wire output format options and downloads. → needs: 2, 4
6. [x] Add tests for settings, redraw flow, and summary rendering. → needs: 1, 2, 3, 4
