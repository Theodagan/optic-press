# App Shell Workspace Tasks

0. [x] use the angular cli to bootstrap the app conforming to architecture describde in the steering docs 
1. [x] Create Angular routes for homepage and each tool workspace. → needs: —
2. [x] Implement homepage tool grid from PRD tool list. → needs: 1
3. [x] Implement shared upload zone with drag/drop, file picker, and paste. → needs: 1
4. [x] Implement shared image card with output status and size stats. → needs: 1
5. [x] Implement shared download bar for aggregate stats and ZIP action hook. → needs: 4
6. [x] Implement shared output options collapsible. → needs: 1
7. [x] Wire placeholder workspaces for all extracted tools. → needs: 2, 3, 4, 6
8. [x] Add route/component smoke tests. → needs: 7
